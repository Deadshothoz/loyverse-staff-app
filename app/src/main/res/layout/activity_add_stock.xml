<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/background">

    <!-- Header with Back button -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:orientation="horizontal"
        android:background="@color/brand_primary"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/backButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:padding="8dp"
            android:text="BACK"
            android:textColor="#FFFFFF"
            android:textStyle="bold"
            android:textSize="15sp"
            android:clickable="true"
            android:focusable="true" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="ADD STOCK"
            android:textColor="#FFFFFF"
            android:textStyle="bold"
            android:textSize="16sp"
            android:gravity="center"
            android:layout_marginEnd="60dp" />
    </LinearLayout>

    <!-- Search box -->
    <EditText
        android:id="@+id/searchInput"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:layout_marginBottom="0dp"
        android:padding="12dp"
        android:background="@drawable/search_box_bg"
        android:hint="Search or scan barcode..."
        android:textColorHint="@color/brand_primary"
        android:textColor="@color/text_primary"
        android:imeOptions="actionSearch"
        android:singleLine="true" />

    <!-- Temporary picker list - shows matches while typing/scanning.
         Tap a row to add it to the working list below. -->
    <ListView
        android:id="@+id/pickerListView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_maxHeight="220dp"
        android:layout_margin="16dp"
        android:layout_marginTop="4dp"
        android:background="@drawable/search_box_bg"
        android:divider="@color/divider"
        android:dividerHeight="1dp"
        android:visibility="gone" />

    <TextView
        android:id="@+id/statusText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="16dp"
        android:paddingTop="4dp"
        android:textColor="@color/text_secondary" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="16dp"
        android:paddingTop="8dp"
        android:text="Items to update:"
        android:textStyle="bold"
        android:textColor="@color/text_primary" />

    <!-- Persistent working list - stays on screen until Confirm is pressed -->
    <ListView
        android:id="@+id/pickerListView"
        android:layout_width="match_parent"
        android:layout_height="220dp"
        android:layout_margin="16dp"
        android:layout_marginTop="4dp"
        android:background="@drawable/search_box_bg"
        android:divider="@color/divider"
        android:dividerHeight="1dp"
        android:visibility="gone" />

    <!-- Confirm button pinned at bottom -->
    <Button
        android:id="@+id/confirmButton"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_margin="16dp"
        android:text="CONFIRM CHANGES"
        android:textColor="#FFFFFF"
        android:textStyle="bold"
        android:backgroundTint="@color/brand_primary" />

</LinearLayout>
